# Set this to the chosen HTTPS blog origin before the first EdgeOne deployment.
# Do not use the old GitHub Pages /Blog prefix on a dedicated blog subdomain.
# SITE_URL=https://blog.your-domain.example

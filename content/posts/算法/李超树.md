---
title: "李超树"
date: 2026-03-05T10:19:54+08:00
banner: "images/post-images/miu_you-no-shu-ju-jie-gou.jpg"
cover: "images/post-images/miu_you-no-shu-ju-jie-gou.jpg"
---

## 李超线段树

当传统的区间变成抽象的线段(一维函数)时,普通的线段树难以维护,此时就需要李超线段树

李超线段树的区别之一是查询不再是简单的单点贡献,而是需要每一层都算一次贡献取最大(延迟更新)

至于修改,按新线段 $f$ 取值是否大于原标记 $g$，我们可以把当前区间分为两个子区间。其中 **肯定有一个子区间被左区间或右区间完全包含**，也就是说，在两条线段中，肯定有一条线段，只可能成为左区间的答案，或者只可能成为右区间的答案。我们用这条线段递归更新对应子树，用另一条线段作为懒标记更新整个区间，这就保证了递归下传的复杂度。当一条线段只可能成为左或右区间的答案时，才会被下传，所以不用担心漏掉某些线段。

具体来说，设当前区间的中点为 $m$，我们拿新线段 $f$ 在中点处的值与原最优线段 $g$ 在中点处的值作比较。

如果新线段 $f$ 更优，则将 $f$ 和 $g$ 交换。那么现在考虑在中点处 $f$ 不如 $g$ 优的情况：

1. 若在左端点处 $f$ 更优，那么 $f$ 和 $g$ 必然在左半区间中产生了交点，$f$ 只有在左区间才可能优于 $g$，递归到左儿子中进行下传；
2. 若在右端点处 $f$ 更优，那么 $f$ 和 $g$ 必然在右半区间中产生了交点，$f$ 只有在右区间才可能优于 $g$，递归到右儿子中进行下传；
3. 若在左右端点处 $g$ 都更优，那么 $f$ 不可能成为答案，不需要继续下传。

除了这两种情况之外，还有一种情况是 $f$ 和 $g$ 刚好交于中点，在程序实现时可以归入中点处 $f$ 不如 $g$ 优的情况，结果会往 $f$ 更优的一个端点进行递归下传。

最后将 $g$ 作为当前区间的懒标记。


下传标记：
```cpp
inline int cmp(double x,double y){
	if(x-y>eps)return 1;
	else if(y-x>eps)return -1;
	else return 0;
}
inline double getval(int x,int k){
	return V[k].first*x+V[k].second;
}
inline void pushdown(int k,int l,int r,int z){
	//cout<<k<<" "<<l<<"  "<<r<<"  "<<z<<"  "<<tr[k]<<endl;
	int mid=(l+r)>>1;
	int x=cmp(getval(mid,z),getval(mid,tr[k]));
	if(x==1 || ((!x)&&(z<tr[k])))swap(tr[k],z);
	x=cmp(getval(l,z),getval(l,tr[k]));
	if(x==1 || ((!x)&&(z<tr[k])))pushdown(k<<1,l,mid,z);
	x=cmp(getval(r,z),getval(r,tr[k]));
	if(x==1 || ((!x)&&(z<tr[k])))pushdown(k<<1|1,mid+1,r,z);
//	cout<<z<<"  "<<tr[k]<<"   "<<x<<"  "<<getval(r,z)<<"   "<<getval(r,tr[k])<<endl;
}
```

拆分线段：
```cpp
inline void update(int k,int l,int r,int lx,int rx,int z){
	if(l>=lx&&r<=rx){
		pushdown(k,l,r,z);
	}else{
		int mid=(l+r)>>1;
		if(mid>=lx)update(k<<1,l,mid,lx,rx,z);
		if(mid<rx)update(k<<1|1,mid+1,r,lx,rx,z);
	}
}
```

查询：
```cpp
inline pair<double,int> Max(pair<double,int>x,pair<double,int>y){
//	cout<<x.first<<"  "<<x.second<<"  "<<y.first<<"   "<<y.second<<endl;
	if(cmp(x.first,y.first)==0){
		if(x.second<y.second)return x;
		else return y;
	}else{
		return max(x,y);
	}
}
inline pair<double,int> query(int k,int l,int r,int x){
	//cout<<l<<"  "<<r<<"  "<<x<<"   "<<getval(x,tr[k])<<"  "<<tr[k]<<endl;
	if(l==r){
		return make_pair(getval(x,tr[k]),tr[k]);	
	}else{
		int mid=(l+r)>>1;
		if(x<=mid)return Max(query(k<<1,l,mid,x),make_pair(getval(x,tr[k]),tr[k]));
		else return Max(query(k<<1|1,mid+1,r,x),make_pair(getval(x,tr[k]),tr[k]));
	}
}
```

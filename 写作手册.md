# SpringWait Blog 写作手册

你只需要写正文、提供图片，然后告诉 Codex「发布这篇」。分类、日期、摘要、图片优化、构建检查和 Git 发布都可以交给 Codex。下面的说明用于你想自己整理 Markdown 时参考，不要求你手动操作构建系统。

## 一篇文章放在哪里

新文章使用 `src/content/blog/`，例如：

```text
src/content/blog/随想/first-post.md
public/images/first-post/cover.jpg
public/images/first-post/photo.jpg
```

不要在旧的 `content/posts/` 中新增文章：那是保留的 Hugo 历史源码，Astro 不会从那里发布。

## 可以直接复制的文章模板

将下面的内容保存为 `.md` 文件。最上方两行 `---` 之间是文章信息；第二个 `---` 后面就是正文。

````markdown
---
title: "我的第一篇文章"
pubDate: 2026-09-05T18:00:00+08:00
category: "随想"
tags:
  - 生活
  - 记录
description: "这篇文章的简短介绍。"
cover: "/images/first-post/cover.jpg"
draft: true
---

这里写开头。文章标题会由网站自动显示，正文无需再重复写一次一级标题。

## 第一节

普通文字直接写。段落之间空一行。

**加粗文字**，以及 [链接文字](https://example.com)。

![对图片内容的说明](/images/first-post/photo.jpg)

## 第二节

> 这里是引用。

- 第一项
- 第二项

```cpp
int main() {
    return 0;
}
```
````

这个模板默认是草稿，不会公开。准备发布时说「发布这篇」，Codex 会将 `draft` 改成 `false` 并完成检查。

## 正文怎么写

- 使用普通 Markdown；二级标题 `##` 和三级标题 `###` 会进入文章目录。
- 代码放进三个反引号组成的代码块，并标明语言，例如 `cpp`、`python`、`js`。行内代码用一对反引号，不要用公式符号包住程序代码。
- 行内公式用 `$a+b$`，独立公式用单独两行 `$$` 包住。公式中的百分号写成 `\%`，避免后面的内容被当作注释。
- 内部文章链接写成 `/posts/随想/first-post/` 这样的站内路径。不能链接到尚未发布的草稿，否则链接检查会提示错误。
- 不需要 HTML、Astro 或 MDX；目前普通 Markdown 已满足文章发布需求。

## 分类和标签怎么设置

`category` 是单个分类，例如 `"算法"`、`"随想"`、`"旅行"`。直接写新的分类名称即可，不需要手动修改导航、索引或分类页面。

`tags` 是可选的多个主题标签：

```yaml
category: "算法"
tags:
  - 数据结构
  - 学习笔记
```

分类由 `category` 字段决定，而不是文件夹名字。建议新文章目录与分类一致，方便管理；已发布文章仅调整分类时，不要随意移动文件，否则默认网址可能改变。让 Codex 处理网址保留或跳转。

## 头图和正文图片怎么设置

头图使用文章信息中的 `cover`：

```yaml
cover: "/images/first-post/cover.jpg"
```

- 实际文件在 `public/images/first-post/cover.jpg`，引用时不写 `public`。
- 不要使用 `C:\...` 等本机路径；图片名称建议用英文、数字和短横线。
- 头图会用于文章封面、列表缩略图和社交分享。建议提供横向图片，重要人物或文字靠近中心，因为列表缩略图会裁切。
- 不指定头图时，文章列表封面、正文头图和社交分享图统一使用首页背景图。只有明确提供或指定头图时才使用单独图片，不额外设计装饰图。
- 正文图片按 `![内容说明](/images/first-post/photo.jpg)` 插入。说明应描述图片内容，而不是只写「图片」。
- 你可以直接提供原图，不必自己压缩。Codex 会生成合适的 WebP 版本、更新路径并保留原图；正文图片也由 Codex 按需处理。现有优化脚本主要处理封面和首页背景，不会自动重写所有正文图片。

## 其他文章信息

| 字段 | 用途 | 是否需要你填写 |
| --- | --- | --- |
| `title` | 文章标题 | 提供标题即可 |
| `pubDate` | 发布日期，示例采用北京时间 `+08:00` | Codex 可补充 |
| `category` | 单个分类 | 可指定，也可让 Codex 归类 |
| `tags` | 多个标签 | 可省略 |
| `description` | 摘要及搜索／分享说明 | 可省略，网站会从正文截取 |
| `cover` | 头图路径 | 可省略 |
| `draft` | `true` 隐藏，`false` 发布；省略时默认发布 | 建议写作期间明确设为 `true` |
| `updatedDate` | 修改日期 | 文章更新时由 Codex 补充 |
| `slug` | 自定义网址中 `/posts/` 后的部分 | 通常不用填写 |

**日期不是定时发布开关。** 当前站点仅依据 `draft` 决定是否公开；未来日期且 `draft: false` 的文章也会在下次构建时发布。

默认情况下，`src/content/blog/随想/first-post.md` 的网址是 `/posts/随想/first-post/`。如设置 `slug: "my-first-post"`，网址会变成 `/posts/my-first-post/`。发表后不要随意改文件路径或 `slug`。

## 暂时隐藏与恢复

按你的要求，迁移的 48 篇历史文章保存在根目录 `隐藏内容/文章/`，配套图片保存在 `隐藏内容/图片/`。它们不在 Astro 内容集合或 `public/` 中，因此不生成文章页面、不进入首页、归档、标签、分类、RSS 或 sitemap，图片也不进入部署产物。直接访问原文章路径会返回 404。

正文和图片没有删除。要恢复，可以告诉 Codex「恢复发布某篇文章」或「恢复全部历史文章」；Codex 会同时移动 Markdown 与该文图片、设置 `draft: false` 并重新构建部署。

**隐藏目录不是隐私保护。** GitHub 仓库及 Git 历史仍然公开，旧部署和搜索引擎缓存也不会立即消失。不要把敏感资料存入这个公开仓库。

## 最省事的发布方式

把文章和原图交给 Codex，例如：

> 发布这篇。标题是《周末散步》，分类放「生活」，用第二张图片作为头图，第一张放在第二段后面。正文不要润色。

Codex 负责整理文章信息、保存图片、核对路径与排版、运行检查和构建、提交并推送。EdgeOne 首次连接 GitHub `main` 后，后续推送会自动部署；在首次连接完成前，合并代码不等于新站已经上线。
